/node_modules
/.claude
/*.tar
/.sbot
dev-dist
tsconfig.tsbuildinfo
/docs

# Secrets — never commit (release.jks, keystore.properties, tauri-updater.key, etc.)
/secrets/

# Belt-and-suspenders: catch stray secret files anywhere in the tree
*.jks
*.keystore
keystore.properties
tauri-updater.key
tauri-updater.key.pub
