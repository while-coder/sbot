{
  "name": "wiki.base",
  "version": "0.0.1",
  "description": "Shared base contract for wiki data-source plugins",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "dependencies": {
    "sbot.plugin": "workspace:*",
    "scorpio.ai": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
