{
  "name": "sbot.plugin",
  "version": "0.0.1",
  "description": "Shared, lightweight plugin SDK for sbot (channel & wiki extensions)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
