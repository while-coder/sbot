{
  "name": "@qingfeng346/sbot",
  "version": "0.0.5",
  "releasenote": "- Added WebFetch tool and web search capability\n- Introduced tool execution permission management\n- Refactored prompts system with per-module splitting and on-demand loading\n- Fixed scheduler timer reliability issues\n- Added model selection and i18n support to the frontend\n- Improved chat page interactions\n- Persist Lark user avatars to the database",
  "description": "Self-hosted AI agent server. Open-source alternative to OpenAI, Claude, and Gemini interfaces. Run LLM agents locally with MCP tool support, memory, multi-channel integrations (Lark/Feishu), and a built-in web UI.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/while-coder/sbot.git"
  },
  "homepage": "https://github.com/while-coder/sbot#readme",
  "bugs": {
    "url": "https://github.com/while-coder/sbot/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "main": "dist/dist/index.js",
  "bin": {
    "sbot": "./dist/dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": true,
    "access": "public"
  },
  "scripts": {
    "build:deps": "pnpm --filter sbot.commons run build && pnpm --filter channel.base run build && pnpm --filter channel.lark run build && pnpm --filter channel.slack run build",
    "dev": "tsx src/index.ts",
    "debug": "pnpm run build:deps && rimraf debug && tsc -p tsconfig.debug.json && node scripts/postbuild.js debug",
    "build": "pnpm --filter sbot-website run build && pnpm run build:deps && rimraf dist && tsc && node scripts/postbuild.js",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@langchain/core": "catalog:",
    "axios": "catalog:",
    "channel.lark": "workspace:*",
    "channel.slack": "workspace:*",
    "commander": "catalog:",
    "cron": "^4.4.0",
    "diff": "^8.0.3",
    "express": "^5.2.1",
    "log4js": "^6.9.1",
    "reflect-metadata": "catalog:",
    "sbot.commons": "workspace:*",
    "scorpio.ai": "workspace:*",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7",
    "turndown": "^7.2.0",
    "ws": "^8.19.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "@types/turndown": "^5.0.5",
    "@types/ws": "^8.18.1",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
