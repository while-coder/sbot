Mark an agenda item done (or close ONE occurrence of a routine).

Occurrence routines: closes ONE instance, NOT the whole routine — to stop the routine, use agenda_cancel. Pass `at` (ISO) when the user pinpoints a specific past time (incl. backfilling missed ones); omit for plain "I just did it".

Lifecycle rules and `at` examples → agenda_wiki.
