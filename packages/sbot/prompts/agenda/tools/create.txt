Create an agenda item — todo, reminder, schedule, routine, or future automation.

Trigger phrases: "remember / remind me later / schedule / every day". Trust dedup: "already exists" = success; do NOT pre-call agenda_list. `triggers` is an array; omit priority unless the user signals urgency.

Edge cases (multi-trigger, dueAt vs trigger, action choice) → agenda_wiki.
