Create an agenda item — todo, reminder, schedule, routine, or future automation.

Trigger phrases: "remember / remind me later / schedule / every day". Trust dedup: "already exists" = success; do NOT pre-call agenda_list. `triggers` is an array; omit completionMode / priority unless overriding inference.

Edge cases (occurrence routines, multi-trigger, dueAt vs trigger, action choice) → agenda_wiki.
