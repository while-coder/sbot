Read the text content of a single file inside a zip archive without extracting it.
- zipPath must be an absolute path to an existing .zip file.
- entryPath is the file path inside the zip (e.g. "src/main.ts").
- Text only (utf-8); binary entries are rejected. Use zip_extract for binary content.
