{
  "name": "channel.lark",
  "version": "0.0.1",
  "description": "Lark/Feishu SDK integration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter scorpio.ai run build && rimraf --glob \"dist/*\" && tsc -b --force"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.58.0",
    "scorpio.ai": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
