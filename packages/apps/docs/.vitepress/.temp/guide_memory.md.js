import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Memory","description":"","frontmatter":{},"headers":[],"relativePath":"guide/memory.md","filePath":"guide/memory.md"}');
const _sfc_main = { name: "guide/memory.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="memory" tabindex="-1">Memory <a class="header-anchor" href="#memory" aria-label="Permalink to &quot;Memory&quot;">​</a></h1><p>Sidebar → <strong>Memory Profiles</strong> (under <strong>Tasks</strong>), then enable it per-agent in the agent edit page → <strong>Memory</strong>.</p><p>Memory is the agent&#39;s automatic long-term memory. After each conversation, a background Selector reviews the full memory catalog for relevant entries, then the <strong>MemoryLLM</strong> reads those entries&#39; complete bodies and applies final changes. On later turns the agent reads memories back via the <code>search_memory</code> and <code>read_memory</code> tools.</p><p>Think of it as the agent learning from every conversation without you having to teach it explicitly — and without bloating the system prompt.</p><h2 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h2><ol><li><strong>Select</strong> — after a conversation idles, the <strong>selector model</strong> matches the transcript against self-contained titles from every global + current-workspace entry. It uses one request while the title catalog fits its budget; for oversized catalogs it compresses the transcript once and matches titles in batches. The Selector never reads bodies. If no selector is configured, the writer model performs this step.</li><li><strong>Extract</strong> — the <strong>writer model</strong> reads the selected entries&#39; complete bodies together with the conversation, then creates, updates, removes, or leaves memories unchanged.</li><li><strong>Read</strong> — on subsequent turns the agent calls: <ul><li><code>search_memory</code> — BM25 keyword lookup across stored memories</li><li><code>read_memory</code> — fetch the full body of a memory by its slug</li></ul></li><li><strong>Maintain</strong> — background jobs keep the store healthy: <ul><li><strong>Consolidate</strong> — merges and de-duplicates related memories</li><li><strong>Reconcile</strong> — re-indexes and prunes stale entries</li></ul></li><li><strong>Delete</strong> — removed memories are moved to <code>.archive/</code> and can be recovered.</li></ol><h2 id="global-and-workspace-memory" tabindex="-1">Global and Workspace Memory <a class="header-anchor" href="#global-and-workspace-memory" aria-label="Permalink to &quot;Global and Workspace Memory&quot;">​</a></h2><p>One Memory Profile contains two memory layers:</p><ul><li><strong>Global</strong> — user preferences, machine context, and general workflows that apply across projects.</li><li><strong>Workspace</strong> — project paths, conventions, module maps, build steps, and architecture decisions isolated by the current <code>workPath</code>.</li></ul><p>A channel still selects only one Memory Profile. Each turn reads global memory together with the current workspace memory. Different <code>workPath</code> values use separate databases and search indexes, so they cannot search or write one another&#39;s entries. Sessions without a configured <code>workPath</code>, including ordinary Web conversations, share <code>~/.sbot/workspace</code> as their default memory workspace instead of writing everything into global memory. The Memory Profile viewer can switch between global memory and previously created workspaces.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>A <strong>Memory Profile</strong> defines how memories are extracted and read. Sidebar → <strong>Memory Profiles</strong> → New:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Name</td><td>Display name for this profile</td></tr><tr><td>Enabled</td><td>Pause the profile without deleting it</td></tr><tr><td>Writer Model</td><td>The authoritative MemoryLLM that reads selected full bodies and applies final changes</td></tr><tr><td>Selector Model</td><td>Optional cheaper model that screens the complete catalog; defaults to Writer Model</td></tr><tr><td>Writer Prompt</td><td>Controls <strong>what</strong> gets extracted</td></tr><tr><td>Read Prompt</td><td>How retrieved memories are formatted back into the prompt</td></tr></tbody></table><p>Then, in an agent → <strong>Memory</strong> section, toggle Memory on and pick the profile. From the Memory Profiles page you can <strong>View Memories</strong>, inspect pending/failed background jobs, retry or delete any failed job, queue <strong>Clean Up Memories</strong>, and use <strong>Sync File Index</strong> to rescan files and indexes.</p><h2 id="memory-vs-notes-vs-wiki" tabindex="-1">Memory vs Notes vs Wiki <a class="header-anchor" href="#memory-vs-notes-vs-wiki" aria-label="Permalink to &quot;Memory vs Notes vs Wiki&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>Who writes?</th><th>When written?</th></tr></thead><tbody><tr><td><a href="./memory.html">Memory</a></td><td>Background MemoryLLM</td><td>After the conversation idles, automatically</td></tr><tr><td><a href="./note.html">Notes</a></td><td>Humans / operators</td><td>Manually, then recalled automatically or via <code>note_search</code></td></tr><tr><td><a href="./wiki.html">Wiki</a></td><td>Humans / wiki source plugins</td><td>Curated pages, then searched/read by agents</td></tr></tbody></table><p>Memory is the most &quot;set and forget&quot; of the three.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/memory.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const memory = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  memory as default
};
