import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agenda（日程）","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/agenda.md","filePath":"zh/guide/agenda.md"}');
const _sfc_main = { name: "zh/guide/agenda.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agenda-日程" tabindex="-1">Agenda（日程） <a class="header-anchor" href="#agenda-日程" aria-label="Permalink to &quot;Agenda（日程）&quot;">​</a></h1><p>侧栏 → <strong>Agenda Profiles</strong>（位于 <strong>Tasks</strong> 分组），再在 Agent 编辑页 → <strong>Agenda</strong> 中按 Agent 开启。</p><p>Agenda 是 sbot 的有状态提醒 / 日程系统。每个日程<strong>条目</strong>（待办、提醒、日程、周期任务或未来自动化）携带一个或多个<strong>触发器</strong>；触发器到点时，sbot 把消息投递到该条目绑定的会话或渠道。</p><p>与 <a href="./heartbeat.html">Heartbeat</a>（固定间隔的提示词循环）不同，Agenda 是 <em>内容驱动</em> 的：条目可以由 Agent 在对话中创建，并在配置 sync 模型后，每轮对话结束自动保持同步。</p><h2 id="条目与触发器" tabindex="-1">条目与触发器 <a class="header-anchor" href="#条目与触发器" aria-label="Permalink to &quot;条目与触发器&quot;">​</a></h2><p>一个日程<strong>条目</strong>包含内容、分类、优先级、可选的截止时间和完成模式。每个<strong>触发器</strong>按计划触发：</p><table tabindex="0"><thead><tr><th>触发器类型</th><th><code>expr</code> 含义</th><th>示例</th></tr></thead><tbody><tr><td><code>absolute</code></td><td>单个 ISO 时间，触发一次</td><td><code>2026-07-01T09:00</code></td></tr><tr><td><code>interval</code></td><td>两次触发间隔的毫秒数</td><td><code>86400000</code>（每 24 小时）</td></tr><tr><td><code>cron</code></td><td>6 字段 cron（<code>秒 分 时 日 月 周</code>）</td><td><code>0 0 9 * * 1-5</code>（工作日 9 点）</td></tr></tbody></table><p>触发时把 <code>message</code> 按其 <code>action</code> 模式投递到绑定的会话 / 渠道，并在 <code>trigger_fire</code> 日志表里记一行（纯审计，不参与调度）。一次性或有限次数的日程正常耗尽后进入<strong>已完成</strong>。一次性 <code>absolute</code> 触发器投递失败会短暂重试；错过宽限时间或重试超期后会停用，且仅当该条目已没有其它启用触发器时，条目才进入<strong>已过期</strong>。已过期条目作为历史保留，不会自动物理删除。</p><h2 id="配置项" tabindex="-1">配置项 <a class="header-anchor" href="#配置项" aria-label="Permalink to &quot;配置项&quot;">​</a></h2><p><strong>Agenda Profile</strong> 是存储 + 可选自动同步。侧栏 → <strong>Agenda Profiles</strong> → 新建：</p><table tabindex="0"><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td>名称</td><td>该 Profile 的显示名称</td></tr><tr><td>启用</td><td>暂停该 Profile，而不删除配置</td></tr><tr><td>Sync 模型</td><td>可选。每轮对话后从对话自动同步日程的模型（留空则关闭同步）</td></tr><tr><td>Sync 提示词</td><td>可选。控制同步行为的提示词文件（缺省为 <code>agenda/sync/default.txt</code>）</td></tr></tbody></table><p>随后在 Agent → <strong>Agenda</strong> 区块开启 Agenda 并选择 Profile。开启后会注册日程工具；配置 sync 模型后，每轮对话都会从对话内容自动校正日程条目。</p><p>AgendaSync 通常用一次模型调用读取本轮对话和全部待处理条目的完整结构。输入超过 Sync 模型预算时，系统复用同一个模型抽取最多 12 条日程变更意图，先对完整 Pending 目录做本地相关性排序，再最多用 6 个 Token 预算批次筛选紧凑卡片。各批候选携带可跨批比较的相关性分数，最终 Writer 最多读取 20 条候选的完整结构；仍超预算时，触发消息改为 60 字符预览。若本轮对话本身过长，会保留开头与结尾并截去中段。Selector 的 <code>shouldSync</code> 只是建议，超预算路径仍会让最终 Writer 复核；单批失败也会回退到本地候选，不会丢掉整轮。因此一次降级同步最多调用模型 8 次（分析 1 次 + 卡片 6 批 + Writer 1 次）。这里没有独立的 Selector 模型配置。</p><p>在 Agenda Profiles 页面 → <strong>查看</strong> 可浏览已存条目、按待处理 / 已完成 / 已取消 / 已过期筛选、手动 <strong>完成</strong> / <strong>取消</strong>，也可以新增 / 编辑 / 停用 / 重新启用 / 删除触发器、手动触发测试，并查看每个触发器的触发历史。恢复已过期条目只会把主体改回待处理；仍需另外调整时间或启用合适的触发器。</p><h2 id="agent-工具" tabindex="-1">Agent 工具 <a class="header-anchor" href="#agent-工具" aria-label="Permalink to &quot;Agent 工具&quot;">​</a></h2><p>开启后，Agent 获得以下工具：</p><table tabindex="0"><thead><tr><th>工具</th><th>用途</th></tr></thead><tbody><tr><td><code>agenda_create</code></td><td>创建带一个或多个触发器的条目</td></tr><tr><td><code>agenda_list</code></td><td>列出当前条目（触发器的 <code>message</code> 截断为预览）</td></tr><tr><td><code>agenda_get</code></td><td>按 id 读单条完整信息：<code>message</code> 全文、已停用的触发器、时间戳，可选触发历史</td></tr><tr><td><code>agenda_edit</code></td><td>一次原子调用改主体字段和/或触发器（add / patch / remove）</td></tr><tr><td><code>agenda_close</code></td><td>终结条目——<code>outcome: done</code>（做完了）或 <code>dropped</code>（不要了）</td></tr><tr><td><code>agenda_wiki</code></td><td>边界场景的工具内参考（多触发器、dueAt vs 触发器、action 选择等）</td></tr></tbody></table><h2 id="agenda-vs-heartbeat" tabindex="-1">Agenda vs Heartbeat <a class="header-anchor" href="#agenda-vs-heartbeat" aria-label="Permalink to &quot;Agenda vs Heartbeat&quot;">​</a></h2><table tabindex="0"><thead><tr><th>需求</th><th>使用</th></tr></thead><tbody><tr><td>有状态的待办 / 提醒 / 日程，从对话自动同步</td><td><strong>Agenda</strong></td></tr><tr><td>一次性、间隔或 cron 触发，并保留投递审计历史</td><td><strong>Agenda</strong></td></tr><tr><td>每隔 N 秒 / 分钟对某 Agent 运行固定提示词</td><td><a href="./heartbeat.html">Heartbeat</a></td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh/guide/agenda.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agenda = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agenda as default
};
