import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Wiki Knowledge Base","description":"","frontmatter":{},"headers":[],"relativePath":"guide/wiki.md","filePath":"guide/wiki.md"}');
const _sfc_main = { name: "guide/wiki.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="wiki-knowledge-base" tabindex="-1">Wiki Knowledge Base <a class="header-anchor" href="#wiki-knowledge-base" aria-label="Permalink to &quot;Wiki Knowledge Base&quot;">​</a></h1><p>Sidebar → <strong>Knowledge (Docs)</strong> → New</p><p>A Wiki is a structured knowledge base of pages (title + content + tags). Agents can search and read assigned wikis during conversations, while the Web UI manages writable sources such as local files. This makes it a good fit for project documentation, runbooks, FAQs, and team-shared references.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Name</td><td>Wiki identifier</td></tr><tr><td>Source Type</td><td>Data source plugin, such as local files or Google Drive</td></tr><tr><td>Source Config</td><td>Extra fields required by the selected source</td></tr><tr><td>Embedding</td><td>Optional — when set, enables hybrid keyword + semantic search; without it, falls back to keyword-only search</td></tr></tbody></table><h2 id="data-sources" tabindex="-1">Data Sources <a class="header-anchor" href="#data-sources" aria-label="Permalink to &quot;Data Sources&quot;">​</a></h2><p>Wiki sources are plugin-based:</p><ul><li><strong>Local files</strong> — the default writable source; pages are stored as Markdown under the wiki cache directory</li><li><strong>Google Drive</strong> — a read-only source that treats a Drive folder as a wiki directory and exports Docs/Sheets/Slides into readable text</li><li><strong>Third-party plugins</strong> — additional wiki providers can be loaded through the plugin system</li></ul><p>Read-only sources hide page creation, editing, and deletion in the Web UI.</p><h2 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h2><p>Pages are indexed by:</p><ol><li><strong>Keyword</strong> (always available) — title + content + tags full-text match</li><li><strong>Semantic</strong> (when embedding configured) — vector similarity for fuzzy/concept queries</li></ol><p>Both signals are merged at query time, so an agent searching for &quot;deploy steps&quot; matches both an exact-title hit and a semantically-related &quot;release procedure&quot; page.</p><h2 id="agent-tools" tabindex="-1">Agent Tools <a class="header-anchor" href="#agent-tools" aria-label="Permalink to &quot;Agent Tools&quot;">​</a></h2><p>Once a Wiki is assigned to a session/channel, the agent automatically gets:</p><ul><li><code>wiki_search</code> — query by keyword and/or semantic similarity</li><li><code>wiki_read</code> — read a full page by id</li></ul><p>Page creation, editing, and deletion are managed from the Wiki page in the Web UI for writable sources.</p><h2 id="assignment" tabindex="-1">Assignment <a class="header-anchor" href="#assignment" aria-label="Permalink to &quot;Assignment&quot;">​</a></h2><p>Wikis can be assigned at multiple scopes (most specific wins):</p><ul><li><strong>Default per agent</strong> — every session/channel using this agent inherits it</li><li><strong>Per session</strong> — override on the chat session</li><li><strong>Per channel</strong> — override per channel</li></ul><h2 id="wiki-vs-notes-vs-memory" tabindex="-1">Wiki vs Notes vs Memory <a class="header-anchor" href="#wiki-vs-notes-vs-memory" aria-label="Permalink to &quot;Wiki vs Notes vs Memory&quot;">​</a></h2><p>See <a href="./note.html#notes-vs-wiki-vs-memory">Notes</a> for a comparison table.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/wiki.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const wiki = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  wiki as default
};
