import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agenda","description":"","frontmatter":{},"headers":[],"relativePath":"guide/agenda.md","filePath":"guide/agenda.md"}');
const _sfc_main = { name: "guide/agenda.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agenda" tabindex="-1">Agenda <a class="header-anchor" href="#agenda" aria-label="Permalink to &quot;Agenda&quot;">​</a></h1><p>Sidebar → <strong>Agenda Profiles</strong> (under <strong>Tasks</strong>), then enable it per-agent in the agent edit page → <strong>Agenda</strong>.</p><p>Agenda is sbot&#39;s stateful reminder / scheduling system. Each agenda <strong>item</strong> (a todo, reminder, schedule, routine, or future automation) carries one or more <strong>triggers</strong>; when a trigger fires, sbot delivers a message to the session or channel the item is bound to.</p><p>Unlike <a href="./heartbeat.html">Heartbeat</a> (a fixed-interval prompt loop) the agenda is <em>content-driven</em>: items can be created by the agent during a conversation, and — with a sync model configured — kept in sync automatically after every turn.</p><h2 id="items-triggers" tabindex="-1">Items &amp; Triggers <a class="header-anchor" href="#items-triggers" aria-label="Permalink to &quot;Items &amp; Triggers&quot;">​</a></h2><p>An agenda <strong>item</strong> has a content, category, priority, optional due date, and a completion mode. Each <strong>trigger</strong> fires on a schedule:</p><table tabindex="0"><thead><tr><th>Trigger kind</th><th><code>expr</code> meaning</th><th>Example</th></tr></thead><tbody><tr><td><code>absolute</code></td><td>A single ISO datetime — fires once</td><td><code>2026-07-01T09:00</code></td></tr><tr><td><code>interval</code></td><td>Milliseconds between fires</td><td><code>86400000</code> (every 24h)</td></tr><tr><td><code>cron</code></td><td>6-field cron (<code>sec min hour day month weekday</code>)</td><td><code>0 0 9 * * 1-5</code> (9am weekdays)</td></tr></tbody></table><p>When a trigger fires it delivers its <code>message</code> to the bound session/channel using its <code>action</code> mode, and the fire is recorded in the <code>trigger_fire</code> log table (pure audit, not used for scheduling). A one-shot or finite schedule that reaches normal exhaustion becomes <strong>Done</strong>. A one-shot <code>absolute</code> trigger retries briefly on delivery failure; if it misses the grace window or gives up retrying, it is disabled and the item becomes <strong>Expired</strong> only when no other trigger on that item remains enabled. Expired items are retained as history rather than automatically deleted.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>An <strong>Agenda Profile</strong> is the store + optional auto-sync. Sidebar → <strong>Agenda Profiles</strong> → New:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Name</td><td>Display name for this profile</td></tr><tr><td>Enabled</td><td>Pause the profile without deleting it</td></tr><tr><td>Sync Model</td><td>Optional. The model that auto-syncs agenda items from the conversation after each turn (leave empty to disable sync)</td></tr><tr><td>Sync Prompt</td><td>Optional. Prompt file controlling sync behavior (defaults to <code>agenda/sync/default.txt</code>)</td></tr></tbody></table><p>Then, in an agent → <strong>Agenda</strong> section, toggle Agenda on and pick the profile. Enabling it registers the agenda tools; with a sync model, items are reconciled from the conversation automatically each turn.</p><p>AgendaSync normally uses one model call with the completed turn and the full structure of every pending item. When that input exceeds the Sync Model budget, it reuses the same model to extract up to 12 agenda-change intents, locally ranks the complete pending catalog, and screens compact cards in at most 6 token-bounded batches. Batch results carry cross-batch relevance scores; the final Writer receives at most 20 selected full records. If those records still exceed the budget, trigger messages become 60-character previews. An oversized turn is middle-truncated while preserving its beginning and end. The Selector&#39;s <code>shouldSync</code> result is advisory—the final Writer still verifies every overflow turn—and one failed card batch falls back to local ranking instead of discarding the entire sync. This bounds an overflow run to at most 8 model calls (analysis + 6 batches + final Writer). There is no separate Selector Model setting.</p><p>From the Agenda Profiles page → <strong>View</strong> you can browse stored items, filter by pending/done/cancelled/expired, manually <strong>Complete</strong> / <strong>Cancel</strong>, add/edit/disable/reopen/delete triggers, fire a trigger manually for testing, and inspect each trigger&#39;s fire history. Reopening an expired item restores only the item to Pending; retime or re-enable an appropriate trigger separately.</p><h2 id="agent-tools" tabindex="-1">Agent Tools <a class="header-anchor" href="#agent-tools" aria-label="Permalink to &quot;Agent Tools&quot;">​</a></h2><p>Once enabled, the agent gets these tools:</p><table tabindex="0"><thead><tr><th>Tool</th><th>Purpose</th></tr></thead><tbody><tr><td><code>agenda_create</code></td><td>Create an item with one or more triggers</td></tr><tr><td><code>agenda_list</code></td><td>List current items (each trigger&#39;s <code>message</code> truncated to a preview)</td></tr><tr><td><code>agenda_get</code></td><td>Read one item in full by id: complete <code>message</code>, disabled triggers, timestamps, optional fire log</td></tr><tr><td><code>agenda_edit</code></td><td>Change item fields and/or triggers (add / patch / remove) in one atomic call</td></tr><tr><td><code>agenda_close</code></td><td>Terminate an item — <code>outcome: done</code> (finished) or <code>dropped</code> (no longer wanted)</td></tr><tr><td><code>agenda_wiki</code></td><td>In-tool reference for edge cases (multi-trigger, dueAt vs trigger, action choice)</td></tr></tbody></table><h2 id="agenda-vs-heartbeat" tabindex="-1">Agenda vs Heartbeat <a class="header-anchor" href="#agenda-vs-heartbeat" aria-label="Permalink to &quot;Agenda vs Heartbeat&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Need</th><th>Use</th></tr></thead><tbody><tr><td>Stateful todos / reminders / schedules, auto-synced from conversation</td><td><strong>Agenda</strong></td></tr><tr><td>One-shot, interval, or cron triggers with delivery audit history</td><td><strong>Agenda</strong></td></tr><tr><td>Run a fixed prompt every N seconds/minutes against an agent</td><td><a href="./heartbeat.html">Heartbeat</a></td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/agenda.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agenda = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agenda as default
};
