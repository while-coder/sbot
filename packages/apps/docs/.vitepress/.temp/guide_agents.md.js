import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Agents","description":"","frontmatter":{},"headers":[],"relativePath":"guide/agents.md","filePath":"guide/agents.md"}');
const _sfc_main = { name: "guide/agents.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="agents" tabindex="-1">Agents <a class="header-anchor" href="#agents" aria-label="Permalink to &quot;Agents&quot;">​</a></h1><p>Sidebar → <strong>Agent Management</strong> → New</p><p>An agent bundles a model, a system prompt, and the tools / skills / knowledge it can reach. You then assign the agent to chat sessions or channels.</p><h2 id="agent-modes" tabindex="-1">Agent Modes <a class="header-anchor" href="#agent-modes" aria-label="Permalink to &quot;Agent Modes&quot;">​</a></h2><h3 id="single" tabindex="-1">Single <a class="header-anchor" href="#single" aria-label="Permalink to &quot;Single&quot;">​</a></h3><p>Pick a model, write a system prompt, optionally attach MCP tools and skills. The default mode for focused single-purpose assistants.</p><h3 id="react" tabindex="-1">ReAct <a class="header-anchor" href="#react" aria-label="Permalink to &quot;ReAct&quot;">​</a></h3><p>Pick a <strong>think model</strong>, then add sub-agents (each with an id and description for task dispatch). The think model decomposes user requests and dispatches sub-tasks recursively; each sub-agent has read-only access to shared memory.</p><p>Use ReAct when:</p><ul><li>The task is open-ended (&quot;plan and execute X end-to-end&quot;)</li><li>You want the orchestrator to choose specialists dynamically</li></ul><p>Each dispatched sub-task can inherit context from the parent conversation (<code>none</code> — clean start, the default; <code>state</code> — a bounded snapshot of recent parent messages; <code>full</code> — the full forked history). Recursion depth is guarded to prevent runaway nesting.</p><h3 id="generative" tabindex="-1">Generative <a class="header-anchor" href="#generative" aria-label="Permalink to &quot;Generative&quot;">​</a></h3><p>Pick a multimodal model for mixed text + image content generation.</p><h3 id="acp" tabindex="-1">ACP <a class="header-anchor" href="#acp" aria-label="Permalink to &quot;ACP&quot;">​</a></h3><p>Run an external Agent Client Protocol process as the agent. Presets are available for Claude Code, Codex, OpenCode, Cline, and Qwen Code; you can also provide a custom command, args, environment variables, session mode, and initialization timeout.</p><p>Use <strong>Persistent</strong> mode to keep the external process alive across turns, or <strong>Transient</strong> mode to close the session after each turn.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Section</th><th>Purpose</th></tr></thead><tbody><tr><td>Model</td><td>Primary LLM for this agent</td></tr><tr><td>System prompt</td><td>Persona, capabilities, response style</td></tr><tr><td>MCP tools</td><td>Per-agent enable list of <a href="./mcp.html">MCP servers</a></td></tr><tr><td>Skills</td><td>Per-agent <a href="./skills.html">skill</a> selection (empty = load all)</td></tr><tr><td>Notes</td><td>Default <a href="./note.html">notes</a> (vector store) for sessions using this agent</td></tr><tr><td>Wiki</td><td>Default <a href="./wiki.html">wiki/knowledge base</a> for sessions</td></tr><tr><td>Memory</td><td>Per-agent long-term memory via background MemoryLLM — see <a href="./memory.html">Memory</a></td></tr><tr><td>Agenda</td><td>Per-agent reminders / schedules, optionally synced from the conversation — see <a href="./agenda.html">Agenda</a></td></tr><tr><td>Heartbeat</td><td>Periodic self-activation — see <a href="./heartbeat.html">Heartbeat</a></td></tr><tr><td>ACP command</td><td>External ACP process command, args, env, session mode, and init timeout for ACP agents</td></tr></tbody></table><h2 id="pre-built-agents" tabindex="-1">Pre-Built Agents <a class="header-anchor" href="#pre-built-agents" aria-label="Permalink to &quot;Pre-Built Agents&quot;">​</a></h2><p>Want to skip the manual setup? Browse the <a href="./agent-store.html">Agent Store</a> for ready-to-install bundles (model + prompt + tools + skills + MCP servers).</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/agents.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const agents = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  agents as default
};
