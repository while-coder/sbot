import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Memory（记忆）","description":"","frontmatter":{},"headers":[],"relativePath":"zh/guide/memory.md","filePath":"zh/guide/memory.md"}');
const _sfc_main = { name: "zh/guide/memory.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="memory-记忆" tabindex="-1">Memory（记忆） <a class="header-anchor" href="#memory-记忆" aria-label="Permalink to &quot;Memory（记忆）&quot;">​</a></h1><p>侧栏 → <strong>Memory Profiles</strong>（位于 <strong>Tasks</strong> 分组），再在 Agent 编辑页 → <strong>Memory</strong> 中按 Agent 开启。</p><p>Memory 是 Agent 的自动长期记忆。每轮对话结束后，后台 Selector 会先浏览完整记忆目录并筛出相关条目，再由 <strong>MemoryLLM</strong> 阅读候选正文并执行最终修改。在后续对话中，Agent 通过 <code>search_memory</code> 和 <code>read_memory</code> 工具把它们读回来。</p><p>可以把它理解为：Agent 从每次对话中自我学习，无需你显式教学，也不会撑爆系统提示词。</p><h2 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h2><ol><li><strong>筛选</strong> —— 对话结束后，<strong>selector 模型</strong> 用本轮对话匹配全部全局记忆和当前工作区记忆的自包含标题。标题在预算内时一次完成；目录过大时只将对话压缩一次，再用简短事实分批匹配标题。Selector 不读取正文；未配置时复用 writer 模型。</li><li><strong>提取</strong> —— <strong>writer 模型</strong> 阅读候选条目的完整正文和本轮对话，再决定新建、更新、删除或不修改。</li><li><strong>读取</strong> —— 后续对话中 Agent 调用： <ul><li><code>search_memory</code> —— 在已存记忆中做 BM25 关键词查找</li><li><code>read_memory</code> —— 按 slug 读取某条记忆的完整内容</li></ul></li><li><strong>维护</strong> —— 后台任务保持记忆库健康： <ul><li><strong>Consolidate（合并）</strong> —— 合并、去重相关记忆</li><li><strong>Reconcile（校正）</strong> —— 重建索引、清理陈旧条目</li></ul></li><li><strong>删除</strong> —— 被删除的记忆移到 <code>.archive/</code>，可恢复。</li></ol><h2 id="全局与工作区记忆" tabindex="-1">全局与工作区记忆 <a class="header-anchor" href="#全局与工作区记忆" aria-label="Permalink to &quot;全局与工作区记忆&quot;">​</a></h2><p>一个 Memory Profile 同时包含两层记忆：</p><ul><li><strong>全局</strong> —— 跨项目生效的用户偏好、机器环境和通用工作方式。</li><li><strong>工作区</strong> —— 按当前 <code>workPath</code> 隔离的项目路径、约定、模块关系、构建步骤和架构决策。</li></ul><p>频道仍然只需选择一个 Memory Profile。每轮对话会同时读取全局记忆和当前工作区记忆；不同 <code>workPath</code> 使用独立的数据库和检索索引，不会互相搜索或写入。未配置 <code>workPath</code> 的会话（包括普通 Web 对话）统一使用 <code>~/.sbot/workspace</code> 作为默认记忆工作区，不会把所有内容都写进全局记忆。Memory Profiles 的查看窗口可以在全局和已创建的工作区之间切换。</p><h2 id="配置项" tabindex="-1">配置项 <a class="header-anchor" href="#配置项" aria-label="Permalink to &quot;配置项&quot;">​</a></h2><p><strong>Memory Profile</strong> 定义记忆如何被提取与读取。侧栏 → <strong>Memory Profiles</strong> → 新建：</p><table tabindex="0"><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td>名称</td><td>该 Profile 的显示名称</td></tr><tr><td>启用</td><td>暂停该 Profile，而不删除配置</td></tr><tr><td>Writer 模型</td><td>阅读候选完整正文并执行最终修改的权威 MemoryLLM</td></tr><tr><td>Selector 模型</td><td>可选的低成本目录筛选模型；不设置时复用 Writer 模型</td></tr><tr><td>Writer 提示词</td><td>控制 <strong>提取什么</strong></td></tr><tr><td>Read 提示词</td><td>召回的记忆如何回填进提示词</td></tr></tbody></table><p>随后在 Agent → <strong>Memory</strong> 区块开启 Memory 并选择 Profile。在 Memory Profiles 页面可以 <strong>查看记忆</strong>、检查 pending / failed 后台任务、重试或删除任意失败任务，也可以用 <strong>整理记忆</strong> 排队合并/压缩现有记忆，用 <strong>同步文件索引</strong> 重新扫描文件与索引。</p><h2 id="memory-vs-notes-vs-wiki" tabindex="-1">Memory vs Notes vs Wiki <a class="header-anchor" href="#memory-vs-notes-vs-wiki" aria-label="Permalink to &quot;Memory vs Notes vs Wiki&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>写入方</th><th>写入时机</th></tr></thead><tbody><tr><td><a href="./memory.html">Memory</a></td><td>后台 MemoryLLM</td><td>对话空闲后自动写入</td></tr><tr><td><a href="./note.html">Notes</a></td><td>人 / 运维者</td><td>手动写入，随后自动注入或通过 <code>note_search</code> 召回</td></tr><tr><td><a href="./wiki.html">Wiki</a></td><td>人 / Wiki 数据源插件</td><td>编纂后的页面供 Agent 搜索 / 读取</td></tr></tbody></table><p>三者中 Memory 最&quot;省心、自动&quot;。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh/guide/memory.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const memory = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  memory as default
};
