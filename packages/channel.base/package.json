{
  "name": "channel.base",
  "version": "0.0.1",
  "description": "Shared base for channel integrations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter scorpio.ai run build && rimraf --glob \"dist/*\" && tsc -b --force"
  },
  "dependencies": {
    "scorpio.ai": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
