{
  "name": "wiki.gdrive",
  "version": "0.0.1",
  "description": "Google Drive wiki data-source plugin for sbot",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "dependencies": {
    "@googleapis/drive": "^20.2.0",
    "wiki.base": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
