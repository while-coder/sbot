{
  "name": "channel.xiaoai",
  "version": "0.0.1",
  "description": "XiaoAi (小米音箱) channel integration via MiNA API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "dependencies": {
    "channel.base": "workspace:*",
    "axios": "catalog:"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
