{
  "name": "scorpio.tools.command",
  "version": "0.0.1",
  "description": "Structured command and script-execution tools for Scorpio agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "dependencies": {
    "scorpio.command": "workspace:*",
    "scorpio.llm": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  },
  "private": true
}
