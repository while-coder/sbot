{
  "name": "scorpio.saver.postgres",
  "version": "0.0.1",
  "description": "PostgreSQL provider for scorpio.saver",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force"
  },
  "dependencies": {
    "pg": "^8.21.0",
    "scorpio.saver": "workspace:*"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  }
}
