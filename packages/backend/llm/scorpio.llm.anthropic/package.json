{
  "name": "scorpio.llm.anthropic",
  "version": "0.0.1",
  "description": "Anthropic model provider for scorpio.llm",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "rimraf dist && tsc -b --force" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.103.0",
    "scorpio.llm": "workspace:*"
  },
  "private": true,
  "devDependencies": { "@types/node": "catalog:", "rimraf": "catalog:", "typescript": "catalog:" }
}
