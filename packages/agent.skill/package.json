{
  "name": "agent.skill",
  "version": "0.0.1",
  "description": "Skill capability plugin for Scorpio agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -b --force && node scripts/copy-assets.cjs"
  },
  "dependencies": {
    "@langchain/core": "catalog:",
    "js-yaml": "^4.2.0",
    "scorpio.ai": "workspace:*",
    "scorpio.di": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "catalog:",
    "rimraf": "catalog:",
    "typescript": "catalog:"
  },
  "private": true
}
